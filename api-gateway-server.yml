server:
  use-forward-headers: true

security:
  basic:
    enabled: false

zuul:
  ignoredServices: '*'
  ignoreSecurityHeaders: false
  retryable: true
  routes:
    account-service: /v1/account-service/**