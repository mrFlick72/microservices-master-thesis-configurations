turbine:
  appConfig: api-gateway-server,account-service
  clusterNameExpression: new String('default')
  combineHostPort: true
  InstanceMonitor:
      eventStream:
        skipLineLogic:
          enabled: false